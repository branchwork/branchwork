apiVersion: v1
kind: Service
metadata:
  name: {{ include "orchestrai.fullname" . }}
  labels:
    {{- include "orchestrai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orchestrai.selectorLabels" . | nindent 4 }}
