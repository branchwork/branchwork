apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "branchwork.fullname" . }}
  labels:
    {{- include "branchwork.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.config.port | quote }}
  EFFORT: {{ .Values.config.effort | quote }}
