node_modules/
dist/
target/
*.db
.env
package-lock.json
screenshots/take-screenshots.mjs
# Claude Code's local state (scheduled-task locks, user settings)
.claude/
# Drafts + ephemera we don't track
devto-article.md
# Playwright e2e runtime / artifacts (web/playwright.config.ts).
web/.playwright-artifacts/
web/.playwright-runtime.json
tests/smoke-artifacts/
# Branchwork runner per-host runtime state (live session sockets, pidfiles, logs).
.branchwork-runner-sessions/
# Branchwork server/runner operational logs (local nohup/cargo-run output).
server.log
runner.log
# Vite dev server output (`pnpm dev` redirects: Makefile dev-web target).
web-dev.log
# Operator scratch dirs — per-host CLI tooling state, never committed.
.bob/
.codex/

# CI-only: prebuilt binaries staged for deploy/Dockerfile.runtime
dist-bin/

# Local redeploy server log
branchwork-server.log
